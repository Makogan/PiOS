\usepackage[utf8]{inputenc}
\usepackage{courier}
\usepackage{xcolor}
\usepackage{textcomp}

\usepackage{pagecolor}

\usepackage{layout}
%\usepackage{showframe}

\usepackage{tikz}
\usepackage[margin = 2cm]{geometry}
\usepackage[document]{ragged2e}

\usepackage{tocloft}

\renewcommand\cftchapaftersnum{.}
\renewcommand\cftsecaftersnum{.}

\renewcommand\thechapter{\Roman{chapter}}
\renewcommand\thesection{\arabic{section}}
\renewcommand\thesubsection{(\alph{subsection})}

\usepackage{lmodern}

\usepackage{scrextend}
\changefontsizes[14pt]{14pt}
		
\font\myfont=cmr12 at 40pt

\usepackage{hyperref}
\hypersetup{
	colorlinks=true,
	linkcolor=blue,
	filecolor=magenta,      
	urlcolor=magenta,
}

\usepackage{fancyhdr}
\fancypagestyle{plain}{%
	\fancyhf{} % clear all header and footer fields
	\fancyfoot[C]{\sffamily\fontsize{15pt}{15pt}\selectfont\thepage} % except the center
	\renewcommand{\headrulewidth}{0pt}
	\renewcommand{\footrulewidth}{0pt}}
\pagestyle{plain}


	
\usepackage{listings}
\usepackage{color}

\definecolor{lightblue}{rgb}{0.7, 0.9, 1}
\definecolor{lightgreen}{rgb}{0.5, 1, 0.5}
\definecolor{darkred}{rgb}{0.5,0,0}
\definecolor{lightpurple}{rgb}{0.8,0.8,1}
\definecolor{skyblue}{rgb}{0.102, 0.75, 1}
\definecolor{lightgray}{rgb}{0.9,0.9,0.9}

\definecolor{dkgreen}{rgb}{0,0.6,0}
\definecolor{gray}{rgb}{0.5,0.5,0.5}
\definecolor{mauve}{rgb}{0.58,0,0.82}
\lstset{frame=tb,
	language=C,
	aboveskip=3mm,
	belowskip=3mm,
	showstringspaces=false,
	columns=flexible,
	basicstyle={\small\ttfamily},
	numbers=none,
	numberstyle=\tiny\color{gray},
	keywordstyle=\color{blue},
	commentstyle=\color{dkgreen},
	stringstyle=\color{mauve},
	breaklines=true,
	breakatwhitespace=true,
	backgroundcolor=\color{lightgray},
	tabsize=3,
	morekeywords={uint32_t},
	keywordsprefix=#
}

\lstdefinestyle{C}
{
	language=C,
	morekeywords={uint32_t},
}

\lstdefinestyle{bash}{
	language=GCC,
	basicstyle=\small\ttfamily,
	numbers=none,
	numberstyle=\tiny,
	numbersep=3pt,
	frame=tb,
	columns=fullflexible,
	backgroundcolor=\color{lightblue},
	aboveskip=6mm,
	belowskip=6mm,
}

\lstdefinelanguage{GCC}{
	keywords=
	{
		arm-
	},
	emphstyle={\color{blue}\bfseries},
	sensitive=false, % keywords are not case-sensitive
	morecomment=[l]{//}, % l is for line comment
	morecomment=[s]{/*}{*/}, % s is for start and end delimiter
	morestring=[b]",
}

\lstdefinelanguage{Linker}{
	emph=
	{
		SECTIONS,
	},
	emphstyle={\color{blue}\bfseries},
	sensitive=false, % keywords are not case-sensitive
	morecomment=[l]{//}, % l is for line comment
	morecomment=[s]{/*}{*/}, % s is for start and end delimiter
	morestring=[b]",
	keywordsprefix=.
}

\lstdefinelanguage{ASM}{
	emph=
	{
		b, ble, blt, bne, 
		ldr, str, 
		r0,	r1, r2, r3, r4, r5, r6, r7, rr8, r9, r10, r11, r12
		sp,
		section, global
	},
	emphstyle={\color{blue}\bfseries},
	sensitive=false, % keywords are not case-sensitive
	morecomment=[l]{//}, % l is for line comment
	morecomment=[s]{/*}{*/}, % s is for start and end delimiter
	morestring=[b]",
} % 

\lstdefinestyle{ASM}{
	language=ASM,
	basicstyle=\small\ttfamily,
	numbers=none,
	numberstyle=\tiny,
	numbersep=3pt,
	frame=tb,
	columns=fullflexible,
	backgroundcolor=\color{lightgreen},
	aboveskip=6mm,
	belowskip=6mm,
	keywordstyle=\ttb\color{orange},
}
